{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}
<h2>Lista de Usuarios: </h2>
<hr>
<a  href="/admin/usuario/add"><button class="btn btn-outline-success mt-2">Adicionar Nova Raridade </button></a>
<hr>

    
<div class="card mt-4">
    <div class="card-body">
        {{#each usuario}}
            <a href="/admin/usuario/edit/{{_id}}" class="list-group-item list-group-item-action mt-2">{{Nome}}</a><br>
        {{else}}
            <h3 class="mt-3">Nenhuma Categoria Registrada </h3>
        {{/each}}
    </div>
</div>
