{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}
<h3>Editar Raridade: </h3>
<div class="card">
    <div class="card-body">      
        <form action="/admin/raridade/edit" method="POST">
        <input type="hidden" name="id" value="{{raridade._id}}">
            <label for="nome">Nome: </label>
                <input type="text" id="nome" name="nome" class="form-control" value="{{raridade.Nome}}" required>
            <br>
            <button type="submit" class="btn btn-outline-primary mt-2">Editar Raridade</button>
        </form>
    </div>
</div>